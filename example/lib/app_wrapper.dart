import 'package:flutter/material.dart';
import 'package:gemira/home_screen.dart';

class AppWrapper extends StatelessWidget {
  const AppWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const HomeScreen();
  }
} 